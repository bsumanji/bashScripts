#/bin/bash

arg1=$1
arg2=$2

if [ -d $arg1 ]
then
	echo "the $arg1 is a directory, closing program now..."
else
	echo "$arg1 is not a directory. Creating a directory in pwd with name $arg2 ..."
	mkdir $arg2
fi
